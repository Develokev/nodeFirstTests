const {arrayColores, arrayColores2} = require('./colores');

arrayColores.forEach((item) => {
    console.log(item);
})

arrayColores2.forEach((item) => {
    console.log(item);
})

//npm START para inicializar el NODE

//npm RUN TEST, para inicializar el TEST

//